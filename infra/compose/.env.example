# .env.example for production compose
# Copy to .env and adjust values before running `docker compose -f docker-compose.prod.yml up -d`

# Optional domain for Caddy automatic HTTPS. Leave blank to skip cert issuance.
DOMAIN=
# Email for ACME certificate registration (recommended if DOMAIN is set)
ACME_EMAIL=
# Optional API key to protect /api/* endpoints (send as header: x-api-key)
API_KEY=
# Override exposed application port on host (container always listens on 8000)
APP_PORT=8001
# Disable global updater if you want external scheduling ("false" to disable)
ENABLE_GLOBAL_UPDATER=true
