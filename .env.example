# Copy this file to .env and set values for production HTTPS
# Your public domain (A/AAAA record should point to the server public IP)
DOMAIN=your.domain.com
# Email for ACME/Let’s Encrypt registration and expiry notices (optional but recommended)
ACME_EMAIL=you@example.com
# App-specific variables
API_KEY=
RATE_LIMIT_PER_MIN=200

# Optional: Dynamic DNS (choose one provider and fill values)
# DDNS provider: 'duckdns' (free subdomain) or 'cloudflare' (own domain)
DDNS_PROVIDER=
DDNS_INTERVAL_SECONDS=300

# DuckDNS (https://www.duckdns.org/): create a subdomain and get your token
DUCKDNS_DOMAIN=
DUCKDNS_TOKEN=

# Cloudflare: create an API token (Zone.DNS Edit) and set zone/record
CLOUDFLARE_API_TOKEN=
CF_ZONE_NAME=example.com
CF_RECORD_NAME=app.example.com
