version: '3.8'

services:
  web:
    build: .
    container_name: newproject_web
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:rw
      - ./data:/app/data:rw
      - ./models:/app/models:rw
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
