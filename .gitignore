# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
download/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*,cover

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# envs
.env
.venv/
venv/
ENV/
env.bak/

# .env in compose directory (Docker Compose v2+)
compose/.env
infra/compose/.env

# VSCode
.vscode/

# OneDrive and system files
Thumbs.db
Desktop.ini
~$*

# models and logs
models/
*.log

# OS files
.DS_Store

# Sensitive files
secrets.env

# Caddy runtime (certs, locks, cache) — do not track
infra/caddy/data/
infra/caddy/config/

# --- Project specific (DISABLED to allow committing data) ---
data/*_last_update.txt
data/*_summary.json
data/auto_registry.json
data/*_short_term_with_lag3.csv
data/index_auto_registry.json

# If you want to keep a tiny sample for docs, place it under data/examples/ and whitelist:
!data/examples/

## Model artifacts now versioned (previously ignored)
# models/*.pkl
# models/*.joblib
# models/*_threshold.*

# Logs / temp
logs/
*.tmp

# Generated local override for Caddy (auto-picked ports)
docker-compose.override.local.generated.yml

# Runtime outputs (do not version control)
data_work/*
!data_work/.gitkeep

# 保留個股 CSV；聚合檔不再使用（若出現可忽略或刪除）

